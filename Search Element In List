#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int s;
    scanf("%d",&s);
    int list[s];
    for(int i=0;i<s;i++){
        scanf("%d",&list[i]);
    }
    int search;
    int flag=0;
    scanf("%d",&search);
    for(int i=0;i<s;i++){
        if(list[i]==search){
            printf("Found");
            flag=1;
            break;
        }
    }
    if(flag==0){
        printf("Not Found");
    }
    return 0;
}
