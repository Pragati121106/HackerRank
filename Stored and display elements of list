#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int s;
    scanf("%d",&s);
    int list[s];
    for(int i=0;i<s;i++){
        scanf("%d",&list[i]);
        printf("%d ",list[i]);
    }
    return 0;
}
